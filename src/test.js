const getUsersData = (users, data) => {
    // Completar la función getUsersData
    let arrayExit
    users.map((user, index) => {
      return(
        arrayExit[index].data
      )
    })
    return arrayExit
  }
  
  module.exports = { getUsersData };